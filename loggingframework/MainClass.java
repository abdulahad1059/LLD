public class MainClass {
    public static void main(String[] args) {
        System.out.print("hi");

//        AbstractLogger debugLogger = new DebugLogger(3, null);
//        AbstractLogger errorLogger = new ErrorLogger(2,debugLogger);
//        AbstractLogger infoLogger = new InfoLogger(1,errorLogger);
//
//        AbstractSubject abstractSubject = new AbstractSubject();
//        abstractSubject.addObserver(1, new InfoObserver());
//        abstractSubject.addObserver(2, new ErrorObserver());
//        abstractSubject.addObserver(3, new DebugObserver());
//        Logger logger = new Logger(infoLogger,abstractSubject);
//        logger.debugLogger("This is info message");
    }
}
